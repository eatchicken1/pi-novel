# Chapter Revision（章节级）

对 diagnose_chapter 的 P0-P3 finding：

- P0：定位到具体事件（如揭示章过早 → 推迟到事件 N；权限非法 → 改走内部审批流程）；
- P1：把 co-occurrence 碰撞升级为 causal/dilemma；给章节补状态变化；
- P2：给女主加代价与错误判断；给配角独立目标；
- P3：重写章尾压力；补中点重释。

修改范围 = affectedEventIds；修改后重跑事件机械检查 + 语义报告 + 装配 + 章节检查。
